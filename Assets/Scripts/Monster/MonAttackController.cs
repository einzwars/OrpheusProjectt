using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MonAttackController : MonoBehaviour
{
    float speed = 0.001f;
    float lifeTime = 3;
    Rigidbody2D monAttrb;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
